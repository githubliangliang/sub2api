# 实施任务

产品代码未开始。规格补全不代表实施或测试完成。每项先用失败测试重现触发条件，再提交修复。
先落第一档并验证，再实施本批；S12a 可独立落地，S12b 等待迁移批准。
现行归属以design.md为准：S07已迁第一档T19，S11仅余#6654/#6916/#7111；共21个PR/11簇。
七项前移只调整原43个PR的分工，第三/第四档不进入实施。

## 准备
- [ ] 记录实际实施起点 SHA 与工作树已有改动，保留 source-baseline.md 与 source-feature-map.md 不改写。
- [ ] 确认第一档完成并验证；在实施起点对本批21项重跑四态，与 evidence-0.2.5/files.tsv 比对偏移。
- [ ] 对每个 CONFLICT / NOBASE 簇核对被改函数与新引用符号；先读 PR 整体 diff，再按本地基座适配。
- [ ] 检查拟用测试的 build tags；不移植仅在 `integration && postgres` 下编译的测试，必要 SQLite 契约以本地可执行测试覆盖。

## 优先：正文、协议、资源与配额正确性

- [ ] S01 #6925：覆盖 done/终态恢复、增量后缀、重复/错位索引、工具与 thinking 生命周期。
- [ ] S01 #7094：合并连续前导指令，中途 system/developer 改 user；保留消息顺序与工具媒体。
- [ ] S02 #6965：覆盖真实空闲 ping、读超时/关闭、消息有序、脏连接淘汰、排队交接与探活竞态。
- [ ] S02 #7064：先修容量公式尊重已配置系数；验证当前默认1.0、显式系数、硬上限、动态开关和零并发；排除无关WS模式UI文案。
- [ ] S03 #6689：三条 Antigravity 路径统一混合工具策略，移除旧注入；覆盖仅内置工具与无工具反例。
- [ ] S05 #6977：冻结名单判定与出站 `[1m]` 去除；覆盖未知名、合法名、显式映射、透传和其它平台。
- [ ] S06 #6661：预检失败/取消/重试/转发释放槽；视频查询固定所属账号，等待不改归属 TTL，普通调度不受影响。
- [ ] S08 #6943：保留客户端 system cache_control；count_tokens 注入后最多 4 个断点。
- [ ] S08 #6995：最终 beta 与消息级 output_config 一致；覆盖空 system 删除、非空/未知块保留、顶层配置不变。
- [ ] S09 #7074：规范窗口优先、历史窗口分类、reset 快照锚点；验证选号与诊断一致。

## 其次：可观察性与其余既有正确性

- [ ] S04 #7076：部分刷新返回 account + warning，前端更新对应列表行并提示；覆盖成功无告警与失败。
- [ ] S09 #7072：耗时返回调用方、总体粘性命中去重；使用可控延迟避免亚毫秒误断言。
- [ ] S09 #6869：接受旧版与完整心跳，拒绝缺配对字段、非法时间、空指令、重复/未知子元素。
- [ ] S10 #6783：只适配已有正文读取路径；固定载荷比较分配字节，覆盖虚报长度、gzip、读取错误与既有归一化回归。
- [ ] S10 #7010：核对隐私池消费者后改 Firefox；验证 cf-mitigated、正文回退与普通响应，保留独立 OAuth 客户端指纹。
- [ ] S11 #6916：分配搜索排除已删除用户，并保留第一档 #7024 的选择失效回归。
- [ ] S11 #7111：代理凭据省略/null/空串/非空端到端语义，适配账号与代理导入调用。
- [ ] S12a #6971：TTFT 数据字段、入口与显示；真实 SQLite 验证降序、并列时间及 NULL 排后，无迁移。

## 后段：既有体验与默认值调优

- [ ] S11 #6654：注册确认密码的缺失/不一致/成功、显隐与禁用状态。
- [ ] S02 #7043：记录容量依据后独立决定是否采用默认5.0；采用时核对示例配置与显式覆盖，不采用时保留1.0并记未实施，不阻塞#7064验收。

## 原有批准边界：平台限额迁移

- [ ] S12b #6954：记录用户对迁移 227 的明确批准；未批准保持本项及以下子项未完成，不影响其它项验收。
- [ ] S12b 获批后：核对实际最大迁移仍为 226 与本地列定义，适配限额初始化/清空/快照更新及前端卡片/重置语义。
- [ ] S12b 获批后：新增本地 227 清理迁移，以真实 SQLite 验证仅清理三档 NULL、保留 0、幂等和不复活软删行。

## 门禁
- [ ] 在 backend/ 执行 `go build ./...`、`go test -tags=unit ./...`、`golangci-lint run ./...`。
- [ ] 在 frontend/ 执行 `pnpm run typecheck`、`pnpm run lint:check`；逐项运行覆盖 S04/S11/S12 前端行为的定向 vitest。
- [ ] 在仓库根目录执行 `make test-frontend-critical`，确认第一档与第二档共享文件的回归均保留。
- [ ] SQLite 方言审计：实际落地 SQL 不含 PG-only 表达式，SQLite 行为测试可复跑。
- [ ] 核对无 Ent/Wire、依赖、新平台和 VERSION 变更；未实施 S12b 时无新迁移且最大编号 226，获批实施时仅新增 227。
- [ ] 填写 verification.md，逐来源行为给 red/green、命令与落地 SHA；记录排除 hunk 与 S12b 是否获批，未实施不得记为通过。
