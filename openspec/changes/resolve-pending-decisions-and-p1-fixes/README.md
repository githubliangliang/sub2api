# resolve-pending-decisions-and-p1-fixes

两批 P0（[0.1.180](../port-upstream-0.1.180-p0-fixes/) / [0.1.183](../port-upstream-0.1.183-p0-fixes/)）
落地之后的下一批：**4 项 P1 修复 + 3 个长期未决决策一次性拍板**。

| # | 内容 | 来源 |
|---|---|---|
| 1 | 修过期的依赖审计例外（`security-scan.yml` 已红约 8 周） | 本仓库自有，非上游 |
| 2 | Grok 模型目录计费与别名（`grok-3-mini` 超收 6.7× / 12×） | 上游 `ed4207a16`，0.1.180 §6.2(a) |
| 3 | 调度「无可用账号」诊断（返回具体 veto reason） | 上游 `3fd66a33b`，0.1.180 §6.3 |
| 4 | 错误日志记录真实上游端点 | 上游 `4795650d`，0.1.183 §4.2 |
| 5 | **决策 9.1**：Grok 默认文本模型 `grok-4.5` → `grok-4.6` | 上游 `39485f2e2` + `f7145c750` |
| 6 | **决策 9.2**：Go `1.26.5` → `1.27.0` + golangci-lint `v2.9` → `v2.13` | 上游 `cbe258fd1` + `73aabc861` |
| 7 | **决策 9.3**：长上下文计费门控 AND → OR | 对齐上游 v0.1.179 起的语义 |

⚠️ 两条必须先读的硬约束：

- **第 6 项会往上改钱**。9.3 改成 OR 后，分组开关默认开、账号开关默认关 ⇒ 此前**没有**被收长上下文
  倍率的请求，之后会被收。这是本批唯一让费用上升的改动，见 `design.md` 决策 5。
- **顺序固定**：`ed4207a16` → `39485f2e2` → `f7145c750`。后者会改前者刚引入的行，
  且 `f7145c750` 是 `39485f2e2` 的数据迁移，不能只合一半。见 `design.md` 决策 2。

阅读顺序：`proposal.md` → `source-baseline.md` → `source-feature-map.md` → `design.md` →
六个 `specs/*/spec.md` → `tasks.md` → `verification.md`。
