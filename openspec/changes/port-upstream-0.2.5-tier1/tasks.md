# 实施任务

本次已完成产品实现；以下状态按实际代码、定向测试和门禁结果更新。工作树未提交。
实施编号/路径以 [implementation-map.md](./implementation-map.md) 为准；验收细节见 verification.md。
按“触发用例 → 修复 → 反例/兼容性回归 → 记录证据”推进；编译失败不算行为复现。
纯布局/HTML 输入约束允许记录直接检查，已有正确路径无需制造失败。

## 1. 实施准备

- [x] 记录实施实际起点 SHA `5803bc1cf`、日期 `2026-09-19` 与工作树已有改动；范围外未提交改动已保留。
- [ ] 按现行22个 PR 重跑正反向 apply 检查；记录整 PR 状态和异常文件，区分 NOBASE 与上下文冲突。
- [ ] 保持 source-* 与 evidence 快照不变；确认现行映射的勘误仍适用于实施起点。
- [x] 核查新增调用、测试 fixture 与构建标签；确认 httpguts 依赖可用且无需依赖变更。
- [ ] 记录 VERSION=1.1.12、最大迁移=226、依赖/lockfile/Ent/Wire 初始状态。

实施编号保持稳定，按以下阶段顺序执行。T17–T23来自原第二档，未增加总范围。

## 2. 优先：身份、调度及数据正确性

- [x] T01：共享 project_id 的两账号 token 隔离；账号键与旧 project 键失效；无 project_id 反例。
- [x] T02：连续 SSE 事件的重复空行复现与修复；验证 data 解包、非空行、心跳/终止行为。
- [x] T05：两个投影白名单保留 6 个键及 0 值；未配置不新增键；丢弃 postgres 集成 hunk。
- [x] T06：trim 前拒绝非法 UA，最终转发入口不泄漏非法字节；合法身份及默认回退不变。
- [x] T07：JWT/管理员暂态查询故障返回 500，包装 not-found 返回 401；API Key 既有分支保持。
- [x] T08：刷新无响应/429/5xx 保留会话并报告不可用；明确拒绝仍登出，成功重放/跨标签与旧会话保护保持。
- [x] T15：关键词不匹配时防抖前清空选择；立即提交不发送旧 user_id；相同邮箱仍可提交。
- [x] T17：兑换成功后 refreshUser 失败只告警，继续订阅/历史处理；兑换本身失败仍走原失败路径。
- [x] T18：导出前冻结查询参数和文件名日期；跨页导出中修改筛选不污染结果。
- [x] T19：agent_message 信封与明文正文按序转成 user；空项不产生消息，reasoning 状态及已有 additional_tools 保持。

## 3. 其次：局部服务与界面正确性

- [x] T03：真实 HTTP transport 复现重复 Accept-Encoding；改映射规范大小写，其它转发规则不变。
- [x] T04：priority 1/5/10 升序选择；同 priority 按 ID 升序；不可用候选仍跳过。
- [x] T09：创建/更新非法峰谷配置返回 400 / INVALID_PEAK_RATE_CONFIG 且不写库；合法配置成功。
- [x] T13：异步验证成功按邮箱身份移除；重排、并发成功和验证失败不误删，手动删除入口保持。
- [x] T14：所有代理筛选入口从靠后页回到第 1 页，分页操作仍保留指定页和 page_size。
- [x] T20：配额重置使用接口返回值更新操作开始时的密钥；等待期间切换到B不得覆写B。
- [x] T21：自动刷新按所选间隔触发，不多等一秒；加载完成按所选间隔重置，关闭状态不被重新开启。
- [x] T22：资料/密码失败复用 extractApiErrorMessage；有效错误可见，未知错误有本地化兜底，成功行为不变。
- [x] T23：部分导入成功后关闭只通知一次刷新；全失败不通知，完整成功关闭不重复，重新打开清空状态。

## 4. 后段：展示与按使用前提生效的既有项

- [x] T10：成本浮层金额/零值为 8 位；0.00000012 显示非零，表格格式和计费值不变。
- [ ] T11：续费弹窗标题可见、列表滚动；代码与 Vitest 已通过，真实登录态浏览器验收待补。
- [x] T12：前后端接受 alipay.qr，拒绝空格/大写/斜杠等非法值；中英提示同步。
- [ ] T16：前端 max=36500；源定义与 typecheck 已核对，真实浏览器输入边界待补。

T12仅在启用EasyPay且使用对应自定义方式时有收益；它仍是既有范围，不因此引入其它支付改造。

## 5. 门禁与交付

- [x] 先运行本批定向回归并记录完整测试名/命令/退出码，再运行后端隔离 build 与目标包回归；共享工作树整包受范围外编译错误阻塞。
- [x] 运行前端 typecheck、lint:check、关键定向 vitest；定向套件覆盖邮箱/订阅竞态及兑换、CSV、agent_message、密钥、自动刷新、错误提取、部分导入。
- [ ] 完成 T11 真浏览器验收及 T16 输入边界核验，不能用 jsdom 类名检查冒充布局验证。
- [x] 核对本批无迁移、Ent/Wire、VERSION、依赖/lockfile 变更；范围外已有工作树改动单独保留。
- [x] 回填 verification.md 的 RED/GREEN、反例、门禁日志和缺口；未完成浏览器项明确标 PARTIAL。
- [x] 更新本 change 状态并记录实际实施起点 SHA；当前产品改动仍为工作树未提交。
